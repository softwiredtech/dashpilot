{
  "$schema": "../../docs/dashapp-manifest.schema.json",
  "manifestVersion": 1,
  "id": "vanilla",
  "perf": {
    "gate": "exempt",
    "reason": "WASM/Bevy 3D renderer does all its work in WebGL/canvas, which the jsdom tripwire cannot observe — the gate would pass trivially regardless of regressions. Needs a renderer-specific gate."
  },
  "settings": [
    "showPhoneBattery", "showCarBattery", "showOdometer",
    "alwaysOnBlindSpotMonitor", "useImperial", "darkMode",
    "renderQuality", "darkModeBackgroundGray"
  ]
}
